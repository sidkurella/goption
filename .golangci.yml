version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - errorlint
    - govet
    - ineffassign
    - nolintlint
    - staticcheck
    - unconvert
    - unused

formatters:
  enable:
    - gofmt
    - goimports
